from django.urls import path
# from .views import (
#     CommentListAPIView,
#     CommentCreateAPIView,
#     RatingListAPIView,
#     RatingCreateAPIView,
# )

urlpatterns = [
    # path('comment/<int:partner_id>', CommentListAPIView.as_view(), name='comment-list'),
    # path('comment/create/', CommentCreateAPIView.as_view(), name='comment-create'),
    #
    # path('rating/<int:partner_id>', RatingListAPIView.as_view(), name='comment-list'),
    # path('rating/create/', RatingCreateAPIView.as_view(), name='comment-create'),
]